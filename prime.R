# Title     : TODO
# Objective : TODO
# Created by: bhargav
# Created on: 15/03/21

start <- as.integer(readline('Enter start: '))
end <- as.integer(readline('Enter end: '))

isPrime <- function(num) {
  for (i in 2:num) {
      if (num %% i == 0) {
        return
      }
  }
  return true;
}

for (i in start:end) {
  if (is)
}